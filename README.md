# A股每日复盘提醒 Chrome 插件

基于 Chrome Manifest V3 的原生浏览器插件，定位为"收盘后复盘打分助手"。支持自选股管理、每日定时提醒、规则评分与报告生成。不依赖任何第三方前端框架，数据全部通过 `chrome.storage.local` 本地持久化。

## 目录结构

```text
├─ manifest.json
├─ assets/icons/
├─ background/service-worker.js
├─ dashboard/
├─ options/
├─ popup/
└─ shared/
   ├─ chart.js
   ├─ defaults.js
   ├─ indicators.js
   ├─ market-api.js
   ├─ report.js
   ├─ rules.js
   ├─ storage.js
   ├─ ui.js
   └─ utils.js
```

## 安装

1. 打开 `chrome://extensions/`
2. 开启右上角「开发者模式」
3. 点击「加载已解压的扩展程序」，选择本仓库根目录
4. 将插件固定到工具栏

## 核心功能

### 自选股管理

- 支持 A 股、港股、北交所
- 弹窗搜索添加（代码 / 名称 / 拼音简写）
- 按得分、涨跌幅排序
- 点击卡片展开 K 线详情面板，再次点击或点空白关闭

### K 线详情面板

- 覆盖式弹出，不撑宽弹窗
- 展示现价、涨跌幅、MA5、BBI、换手率
- 45 日 K 线图（含 MA5 / BBI 均线、成交量柱）
- 当日加分项 / 减分项规则命中详情

### 每日复盘打分

- 预设打分规则，每条默认 1 分，支持自定义分值
- 规则类型包括：
  - 收盘价高于昨日收盘
  - 不破 BBI
  - 无巨量阴线
  - **趋势向上（线性回归）**：取最近 N 日收盘价做最小二乘回归，斜率达标且 R² ≥ 阈值才判为趋势向上，有效过滤横盘震荡股
  - KDJ 未死叉
  - MACD 金叉 / 死叉
  - 均线多头排列等

### 定时提醒

- 默认 15:10 / 16:15 自动刷新行情并生成复盘报告
- 使用 `chrome.alarms` + `chrome.notifications` 推送桌面通知
- 支持手工维护节假日例外

### 行情数据

| 数据 | 来源 |
|------|------|
| 股票搜索 | 新浪财经 suggest |
| 实时行情 | 东方财富个股接口 |
| 历史日 K | 东方财富 push2his |
| 分时数据 | 东方财富 push2 |

缓存策略：历史日线 6 小时 TTL，请求间隔 220ms 防限流，失败时回退旧缓存。

## 规则引擎扩展

新增规则只需三步：

1. 在 `shared/defaults.js` 的 `RULE_TYPES` 加新类型
2. 在 `shared/rules.js` 的 `evaluateRule()` 加对应 case
3. 若依赖新指标，在 `shared/indicators.js` 扩展后挂到 `attachIndicators()`

## 版本记录

### v1.2.9
- 修复：v1.2.8 腾讯日K兜底后行情刷新显示 +0.00%（腾讯接口只返回 OHLCV，原实现涨跌幅填 0），现基于相邻K线计算 change/changePct/amplitude，行情刷新改为取 2 根K线保证有前收盘可算

### v1.2.8
- 修复：扩展 Service Worker 中东财 `get-stock-history` 持续 `TypeError: Failed to fetch`（node/浏览器实测东财可达但扩展环境不可达），为日K新增腾讯 `web.ifzq.gtimg.cn` 兜底数据源，东财失败时自动切换（A股/港股均支持，成交量自动÷100对齐“手”单位）
- 增强：东财日K失败时输出底层网络错误码（`error.cause`），便于在 SW console 定位失败原因

### v1.2.7
- 修复：详情页拉取最新日K偶发 `Failed to fetch`，`fetchEastMoneyHistory` 补充东财 `ut` 参数并增加一次失败重试，降低东财限流/网络抖动导致的瞬时失败

### v1.2.6
- 修复：详情页日K异步拉取结果被 storage 触发重建的 detailRenderState 丢弃（改为按股票代码判断），打开详情时优先保留已刷新的最新日K，彻底解决收盘后下影线等形态缺失
- 修复：Dashboard 日K同样受 6h 缓存影响，新增 refreshDailyForSelection 在选择股票/切到日K/初始加载时异步拉取最新日K并优先渲染

### v1.2.5
- 修复：详情页日K使用 6 小时 TTL 缓存，收盘后仍显示盘中快照导致下影线等形态缺失，改为打开详情/切换股票/点击日K时异步拉取最新历史K线并重绘
- 修复：manifest 版本号修正（1.1.1 → 1.2.5）

### v1.2.4
- 新增：苹果磨砂玻璃质感升级——浅色/深色壁纸弥散光斑、高饱和 vibrancy、玻璃边缘高光、内层卡片垂直渐变

### v1.2.3
- 性能优化：毛玻璃主题性能优化（移除无限动画与嵌套 blur，静态渐变替代）

### v1.2.2
- 修复：轮询增加交易时段守卫，消除夜间/周末每 15 分钟无效唤醒与行情请求

### v1.2.1
- 修复：后台缓存刷新全局互斥，统一串行化读缓存→刷新→写缓存，四个入口收口避免并发覆盖 marketCache

### v1.2.0
- 性能优化：`realtime-refresh` 移除强制刷新，让 2 分钟行情 TTL 生效，修复 Dashboard 每 5 秒轮询时对全部自选股重复拉行情的最大瓶颈
- 性能优化：popup 自选股列表改用容器级事件委托，消除每次渲染重建 innerHTML 后的重复监听绑定
- 性能优化：评分排行查找改用 Map 缓存，消除循环内线性扫描
- 性能优化：移除详情面板过期的 rAF 图表绘制，统一由渲染流程绘制，修复过期帧闪烁

### v1.1.9
- 新增：popup 排序状态持久化到设置，下次打开自动保持上次选择的排序方式

### v1.1.8
- 修复：设置页清空阈值误存为 0（`??`→`||`），规则表单提交也纳入串行化队列防并发覆盖
- 修复：报告 HTML 导出无行情时现价/涨跌幅显示 `--`（与 Markdown 一致），加分/未命中项加空数组兜底防崩溃
- 修复：Dashboard 选中改用完整 `market+code` 复合键，修复跨市场同码串股；切换分时/选股时重置十字线
- 修复：新浪港股日期兼容 `YYYY/MM/DD` 斜杠格式并规范化，时间兼容无秒的 `HH:MM`
- 修复：K 线图极窄画布时提前返回，避免负宽绘制
- 修复：盘中预警弹窗解析结果校验为数组，防非数组抛错白屏

### v1.1.7
- 修复：刷新闪烁（闪 2 次）——storage 渲染抑制改为覆盖整个异步流程后再计时，避免抑制提前解除触发二次渲染

### v1.1.6
- 修复：新浪 A 股成交量单位（股→手 ÷100）与东财日 K 对齐，保证量比/量增规则口径一致，避免量比被放大 100 倍

### v1.1.5
- 健壮性：修复 `forceQuotes` 死参数，强制刷新内部直接生效
- 健壮性：股票池拉取单源容错，某市场接口失败不拖垮整个搜索补全
- 健壮性：报告无行情时显示 `--` 而非 `0.00`；评分满分为 0 时强弱计数置 0 避免重复统计
- 健壮性：预警通知内存级 2 分钟去重兜底，避免并发任务重复推送
- 健壮性：盘中预警弹窗优先定位主显示器；5 分钟无操作自动关闭防残留窗口
- 健壮性：设置页规则读改写串行化，修复快速连点丢失变更

### v1.1.4
- 修复：日 K 十字线错位（popup / dashboard 映射改为与绘图一致的绘图区）
- 修复：分时数据异步写入过期状态对象，await 后校验当前状态
- 修复：分时无数据回退日 K 时同步切换 tab 高亮
- 修复：搜索输入乱序（请求序号丢弃过期结果）
- 修复：刷新按钮连点并发（spinning 锁）
- 修复：跨市场同代码串股（选中改用完整 symbol）

### v1.1.3
- 毛玻璃主题质感定稿：明亮浅色底 + 高饱和蓝/青/紫光斑漂移，面板更透、模糊更强，呈现 macOS 通透质感

### v1.1.2
- 毛玻璃主题光斑去粉去金：dashboard / options / popup 光斑统一为纯蓝色调，移除粉色与金色

### v1.0.7
- 新增：盘中低分弹窗预警，每个交易日 14:50 / 16:10 扫描自选股，得分 ≤ 阈值时弹出独立浏览器窗口提醒（窗口出现在屏幕右上角，内容可滚动）
- 新增：设置页可配置"开启盘中低分提醒"开关与得分阈值（默认 ≤ 3 分），并新增 `INTRADAY_ALERT_1450` / `INTRADAY_ALERT_1610` 闹钟
- 新增：popup 排序模式扩展为"得分↓ / 得分↑ / 涨跌"三档循环切换
- 新增：`manifest.json` 申请 `system.display` 权限，用于弹窗定位到屏幕右上角
- 修复：MACD 指标 DEA 计算改为只对非空 DIF 做 EMA，避免暖机期被零值污染；同时修正 warmup 长度
- 修复：MACD 金叉判定改为"最近一次交叉为金叉"而非"出现过金叉且未死叉"，更贴合实战语义
- 修复：`close_above_open` 规则优先取实时行情的 `prevClose`，缺失时回退到历史 K 线昨收
- 改进：`uid()` 改用 `crypto.randomUUID()` 生成，碰撞概率更低
- 改进：dashboard tab 按钮 active 态选择器加权，避免被 `.ghost-btn` 覆盖

### v1.0.6
- 安全加固：统一所有页面的 HTML 转义函数，消除潜在 XSS 风险（dashboard、options、report 均改用 `utils.js` 的 `escapeHtml()`）
- 安全加固：`manifest.json` 新增显式 `content_security_policy`，限定 `script-src 'self'`
- 修复：实时刷新 `setInterval` 添加并发保护，防止请求堆积
- 修复：`dedupeTriggeredHits()` 日期比较改用 ISO 字符串直接切片，消除时区歧义导致的重复预警推送
- 修复：`intradayBySymbol` 缓存限制最多保留 10 只股票的分时数据，防止长期运行内存无限增长
- 修复：规则参数（period / lookback / threshold 等）添加合理范围校验，防止异常输入
- 修复：`refreshIntervalMinutes` 设置项添加范围校验（1–1440 分钟）
- 修复：`fetchEastMoneyHistory()` 的 `limit` 参数 clamp 到 `[1, 500]`，防止构造异常 URL
- 修复：`storage.js` 的 `ensureDefaults()` 添加 try-catch，存储异常时优雅降级
- 改进：静默 `.catch(() => {})` 全部替换为 `console.warn` 输出，便于问题排查

### v1.0.5
- K 线图支持分时/日K切换，popup 详情面板新增分时折线图
- 日K悬浮十字光标，显示日期、收盘价、涨跌幅、成交额
- 分时图修复成交量累计值问题，改为每分钟增量
- Canvas 高分辨率屏幕适配（DPR 缩放）
- 自选股卡片重构：市场标识改为名字旁角标，返回键改为图标，叉叉改为右上角悬浮
- 得分 badge 颜色梯度：高分红 / 中分橙 / 低分绿，符合 A 股涨跌色习惯
- 添加自选股表单默认折叠，点击展开详细字段
- Dashboard 行情指标卡改为 3 列布局，K 线图高度增加

### v1.0.4
- 新增 `run-review-cached` 消息处理：popup 打开后先用缓存数据生成复盘报告，避免等待行情刷新
- 移除安装时的自动刷新行情，减少插件首次加载的网络请求
- 移除 popup 初始化时多余的 `loadStockUniverse()` 调用
- 新增 `escapeHtml()` 工具函数，防范 XSS 注入风险

### v1.0.3
- 趋势向上规则改为线性回归算法（斜率 + R² 双条件），有效过滤横盘震荡股
- 修复"收盘上涨"规则：改为今收 > 昨收，而非今收 > 今开

### v1.0.2
- popup 改为覆盖式 K 线详情面板，点空白/拖拽条关闭
- 自选股支持按得分、涨跌幅排序
- 顶部三按钮换为统一 SVG 图标，刷新时旋转动画
- 指标卡"收盘"替换为"换手率"
- 滚动条美化、股票名称自适应宽度
- 更新扩展图标

### v1.0.1
- 初始版本
